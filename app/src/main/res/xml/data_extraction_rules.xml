<?xml version="1.0" encoding="utf-8"?>
<!--
    Android 12+ replaced fullBackupContent with dataExtractionRules.
    Both cloud (Auto Backup) and device-transfer extraction are denied
    explicitly here. With android:allowBackup="false" the cloud path is
    already shut, but the device-transfer path is a separate channel and
    we close that too.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
